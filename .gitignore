# ── Umum ──
.DS_Store
*.log
.env
.env.*
!.env.example

# ── Node ──
node_modules/
dist/
coverage/

# ── PocketBase (sprint 9) ──
# binary versi pin diunduh via `make pb-install`; data lokal via pb-serve.
pocketbase/pocketbase
pocketbase/pb_data/
pocketbase/*.zip

# ── Android (Capacitor) ──
mobile/android/.gradle/
mobile/android/build/
mobile/android/app/build/
mobile/android/local.properties
mobile/android/app/src/main/assets/capacitor.config.json
mobile/android/app/src/main/assets/public/
*.apk
*.aab
*.keystore
!debug.keystore

# ── Editor ──
.idea/
.vscode/
